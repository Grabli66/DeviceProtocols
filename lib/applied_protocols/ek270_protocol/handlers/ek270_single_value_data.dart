/// Единичное значение с типом измерения
class EK270SingleValueData {
  /// Значение
  final String value;

  /// Единица измерения
  final String measureType;

  /// Конструктор
  EK270SingleValueData(this.value, this.measureType);
}
